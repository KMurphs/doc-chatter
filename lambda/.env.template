AWS_REGION=us-east-1
RUST_LOG=info
DEFAULT_MODEL=sonnet
ACCOUNT_ID=<account-id>

SESSIONS_BUCKET=doc-chatter-sessions-$ACCOUNT_ID

# From sam deploy output
API_GW_URL=https://<API_ID>.execute-api.us-east-1.amazonaws.com/prod
COGNITO_USER_POOL_ID=us-east-1_XXXXXXXXX
COGNITO_CLIENT_ID=<CLIENT_ID>
COGNITO_REGION=us-east-1

# Test user (created via scripts or console)
COGNITO_USERNAME=your@email.com
COGNITO_PASSWORD=YourPassword123!
